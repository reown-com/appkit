import { css } from 'lit'

export default css`
  .contentContainer {
    height: 280px;
  }

  .contentContainer > wui-icon-box {
    width: 40px;
    height: 40px;
    border-radius: var(--apkt-borderRadius-3);
  }

  .contentContainer > .textContent {
    width: 65%;
  }
`
