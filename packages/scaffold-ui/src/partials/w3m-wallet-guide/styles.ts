import { css } from 'lit'

export default css`
  wui-flex {
    width: 100%;
  }

  .wallet-guide {
    width: 100%;
  }

  .chip-box {
    width: fit-content;
    background-color: var(--apkt-tokens-core-glass010);
    border-radius: var(--apkt-borderRadius-20);
  }
`
