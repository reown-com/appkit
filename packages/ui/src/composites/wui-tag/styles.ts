import { css } from 'lit'

export default css`
  :host {
    display: block;
    padding: 3.5px 5px 4.5px 5px !important;
    background-color: var(--local-bg-value);
    border-radius: var(--wui-border-radius-5xs);
    color: var(--local-color-value);
  }
`
