import { css } from 'lit'

export default css`
  :host {
    position: relative;
    display: inline-block;
  }

  wui-icon {
    cursor: pointer;
  }

  wui-text {
    margin: 6px 14px 0px 14px;
  }
`
