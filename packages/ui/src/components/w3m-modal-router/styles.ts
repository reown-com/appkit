import { css } from 'lit'

export default css`
  .w3m-modal-router {
    overflow: hidden;
    will-change: height;
    transform: translate3d(0, 0, 0);
  }

  .w3m-modal-router-content {
    display: flex;
    flex-direction: column;
  }
`
