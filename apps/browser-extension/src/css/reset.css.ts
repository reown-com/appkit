import { style } from '@vanilla-extract/css'

const button = style({
  appearance: 'none',
  background: 'none',
  border: 'none',
  cursor: 'pointer',
  textAlign: 'left',
  outline: 'none'
})

export const element = {
  button
}
